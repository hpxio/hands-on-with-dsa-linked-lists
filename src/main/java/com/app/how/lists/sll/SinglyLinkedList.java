package com.app.how.lists.sll;

public class SinglyLinkedList {

    /* Linked-List Node (simple string) */
    class Node {
        String data;
        Node next;

        Node() {
            data = null;
            next = null;
        }
    }

    /* Initially List is empty */
    Node head = null;

    /* Insert new element in the list */
}